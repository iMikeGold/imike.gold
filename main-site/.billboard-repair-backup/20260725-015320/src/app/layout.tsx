import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  metadataBase: new URL("https://imike.gold"),
  title: "(i), Mike Gold — Choose How You Enter",
  description:
    "Enter the system, explore the works, or meet the manchild behind the machinery.",
  openGraph: {
    title: "(i), Mike Gold",
    description: "Choose how you enter.",
    url: "https://imike.gold",
    siteName: "BillBoard OS",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        {children}
      </body>
    </html>
  );
}
